const fs = require('fs');

// fs.mkdirSync('thapa')

// fs.writeFileSync('thapa/bio.txt', "Writing the first line")

// fs.appendFileSync('thapa/bio.txt', " Writing the appended text");

// const direct_text = fs.readFileSync('thapa/bio.txt', 'utf-8');// using utf-8 we can directly get the file text instead buffer data
// console.log(direct_text);

// fs.unlinkSync("thapa/bio.txt"); // to delete the file

// fs.rmdirSync("thapa")